
import { innerHTML } from 'em/helpers/inner-html';
import { module, test } from 'qunit';

module('Unit | Helper | inner html');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = innerHTML([42]);
  assert.ok(result);
});

